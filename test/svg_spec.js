const expect = require('chai').expect;
const { toViewBoxCoords } = require('../src/svg');
const svgConstants = require('../src/svg_constants');

describe('svg', () => {
  describe('toViewBoxCoords', () => {
    // TODO Write some specs for toViewBoxCoords, then implement the method
    // in svg.js and get them all to pass
  });
});
